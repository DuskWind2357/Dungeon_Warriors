# Dungeon Warriors 项目宪章

---

## 一、项目介绍

### 项目概述
**Dungeon Warriors** 是一款 2D 像素风格的地牢肉鸽（Roguelike）游戏。玩家需要挑战 30 层高塔，击败最终首领「高塔之主」通关。

### 核心特色
- **双武器系统**：近战+远程双武器槽，自动切换，策略丰富
- **30层地牢**：每层随机生成地图，10层/20层头目，30层最终Boss
- **多房间楼层**（V1.0.5+）：出生点房间+分支房间架构，副本/宝藏室/特殊宝藏室，传送门连接
- **肉鸽元素**：随机掉落装备、消耗品，每局体验不同
- **丰富装备**：公式化武器+6把特殊武器、5类护甲+4种特殊护甲
- **近战连击&战斗特效**（V1.0.5.12）：剑/斧/矛三段、匕首五段连击，长按连发，剑气/屏抖/闪烁
- **难度系统**：默认/冒险/末日三档难度
- **HUD升级**（V1.0.6）：顶部BOSS血条系统（高塔之主480px/头目160px）
- **生命上限惩罚**（V1.0.6.1）：楼层重置扣10%上限、中途退出扣5%，保底50%
- **存档系统**：JSON格式存档，支持中途退出继续

### 技术栈
| 项 | 值 |
|----|-----|
| 语言 | Python 3.11（conda 环境 Game） |
| 引擎 | Pygame 2.6.1（pygame.mixer 音频，24通道） |
| 窗口 | 960×720, 60 FPS |
| 地图 | 20×15 网格, TILE_SIZE=48, V1.0.5+ 多房间架构 |
| 架构 | 场景状态机 + dataclass 实体 + 系统模块 |
| 打包 | PyInstaller 6.21（spec 文件，windowed exe + UPX） |

### 项目地址
```
GitHub SSH: git@github.com:Dusk_Wind/Dungeon-Warriors.git
本地路径: C:\Users\Dusk_Wind\Desktop\Project\Dungeon Warriors
```

---

## 二、环境搭建

### 开发环境要求
- **Python**: 3.11（conda 环境 `Game`）
- **Pygame**: 2.6.1
- **PyInstaller**: 6.21（仅打包需要）
- **Git**: 最新版

### 安装步骤

```bash
# 1. 克隆仓库（SSH方式）
git clone git@github.com:Dusk_Wind/Dungeon-Warriors.git
cd Dungeon-Warriors

# 2. 激活 conda 环境（本机已建 Game 环境，Python 3.11.15）
conda activate Game
# 如需新建: conda create -n Game python=3.11

# 3. 安装依赖
pip install pygame>=2.6 pyinstaller

# 4. 运行游戏（开发模式）
& "C:\Users\Dusk_Wind\.conda\envs\Game\python.exe" main.py
```

### 打包为可执行文件
```bash
# 打包命令（使用 spec 文件）
& "C:\Users\Dusk_Wind\.conda\envs\Game\python.exe" -m PyInstaller DungeonWarriors.spec --noconfirm
```

---

## 三、Git 分支与提交规范

### 分支命名规范

| 分支类型 | 命名格式 | 说明 |
|---------|---------|------|
| 主分支 | `main` | 稳定发布版本 |
| 开发分支 | `dev` | 开发主线 |
| 功能分支 | `feature/xxx` | 新功能开发 |
| 修复分支 | `fix/xxx` | Bug修复 |
| 重构分支 | `refactor/xxx` | 代码重构 |
| 文档分支 | `docs/xxx` | 文档更新 |

### 分支工作流
```
main (稳定版)
  ↑
dev (开发主线)
  ↑
feature/xxx (功能分支)
  ↑
fix/xxx (修复分支)
```

### 提交信息规范

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type 类型

| 类型 | 说明 |
|------|------|
| `feat` | 新功能 |
| `fix` | Bug修复 |
| `refactor` | 重构（不改变外部行为） |
| `perf` | 性能优化 |
| `docs` | 文档更新 |
| `style` | 代码格式（不影响逻辑） |
| `test` | 测试相关 |
| `chore` | 构建/工具链更新 |

### Scope 范围

```
combat     - 战斗系统
entity     - 实体（玩家/怪物/物品）
inventory  - 背包系统
render     - 渲染相关
audio      - 音效系统
scene      - 场景管理
data       - 静态数据
save       - 存档系统
```

### 示例

```bash
# 新功能
git commit -m "feat(combat): 添加暴击音效和屏幕提示"

# Bug修复
git commit -m "fix(inventory): 修复背包物品无法正确使用的问题"

# 重构
git commit -m "refactor(scene): 将场景调度逻辑抽取到状态机模块"
```

### 提交频率
- 每完成一个独立功能/修复即提交
- 单次提交代码量建议 50-200 行
- 避免提交未完成的代码

---

## 四、代码命名与注释规范

### 命名规范

#### 文件命名
```
模块功能.py          # 小写下划线，如 combat.py
场景_场景名_scene.py  # 场景文件加 _scene 后缀
系统_系统名.py       # 系统文件加前缀
```

#### 类命名
```python
# PascalCase（大驼峰）
class CombatScene:      # 场景类
class Player:           # 实体类
class AudioManager:     # 系统类
class Weapon:           # 数据类
```

#### 变量/函数命名
```python
# snake_case（小写下划线）
player_hp = 100
current_floor = 1

def calculate_damage():
def handle_event():
```

#### 常量命名
```python
# UPPER_SNAKE_CASE（全大写下划线）
WINDOW_WIDTH = 960
FPS = 60
MAX_REVIVES = 3
```

#### 私有方法/属性
```python
# 单下划线前缀
def _init_menu(self):
    self._burn_dmg = 0
```

### 注释规范

#### 文件头部注释
```python
"""
Dungeon Warriors — 模块功能简述
详细说明（可选）
"""
```

#### 类注释
```python
class Game:
    """游戏主类 — 状态机调度"""
```

#### 函数注释
```python
def _new_game(self) -> None:
    """开始新游戏"""
```

#### 行内注释
```python
# 说明当前代码的作用
pygame.key.stop_text_input()  # 禁用中文输入法，防止WASD被拦截
```

#### TODO注释
```python
# TODO: 待实现的功能
# FIXME: 已知问题
# HACK: 临时解决方案
```

---

## 五、功能模块目录说明

```
Dungeon Warriors/
├── main.py              # 入口文件，游戏主循环、状态机调度
├── config.py            # 全局常量配置（窗口/数值/颜色/难度等）
├── utils.py             # 工具函数（资源路径解析，兼容PyInstaller打包）
│
├── data/                # 静态数据层
│   ├── weapons.py       # 武器数据（公式化生成+6把特殊武器）
│   ├── armor.py         # 护甲数据（5类普通×5级+4种特殊）
│   ├── monsters.py      # 怪物数据（普通/精英/雪地精英/头目/首领/宝箱/试炼刷怪笼）
│   ├── consumables.py   # 消耗品数据（面包/4种药水）
│   └── keys.py          # 钥匙道具（藏宝室钥匙）
│
├── entities/            # 实体数据类
│   ├── player.py        # 玩家实体（HP成长/武器/护甲/buff/状态效果/生命上限惩罚）
│   ├── monster.py       # 怪物实体（属性/技能/阶段/伤害上限/锁血）
│   └── item.py          # 物品实体（Weapon/Armor/Consumable/KeyItem）
│
├── systems/             # 核心系统层
│   ├── combat.py        # 战斗计算（伤害/连击/暴击/斩杀/吸血/过热）
│   ├── inventory.py     # 背包系统（64格存储/物品管理/消耗品使用）
│   ├── floor_manager.py # 楼层生成（V1.0.5多房间/BOSS楼层/宝藏室/副本/陷阱）
│   ├── pathfinding.py   # A*寻路（曼哈顿启发式/Bresenham视线简化）
│   ├── save_system.py   # 存档系统（JSON v2，防御式反序列化）
│   ├── audio_manager.py # 音频管理（单例：怪物音效+BGM优先级）
│   └── revive_system.py # 复活系统（3条生命）
│
├── scenes/              # 场景层（状态机）
│   ├── menu_scene.py    # 主菜单
│   ├── settings_scene.py# 设置（难度/低级装备销毁/音乐开关）
│   ├── combat_scene.py  # 战斗场景（核心逻辑：连击/刷怪/传送门/BOSS战）
│   ├── backpack_scene.py# 背包界面（8×8）
│   ├── reward_scene.py  # 通关奖励四选一
│   ├── death_scene.py   # 死亡界面（复活倒计时）
│   ├── game_over_scene.py # 游戏结束
│   └── victory_scene.py # 通关胜利（按任意键退出）
│
├── rendering/           # 渲染层
│   ├── renderer.py      # 渲染器（地图/精灵/HUD/顶部BOSS血条/剑气特效/字体）
│   └── pixel_style.py   # 像素风格工具函数
│
├── music/               # 背景音乐（大厅BGM/地牢/雪地/地狱/BOSS/头目/高塔之主/奖励）
├── sounds/              # 音效资源（按怪物类型分目录 + 玩家/传送门/UI/unlock）
├── icon/                # 图标资源（怪物头像/物品图标/方块贴图/exe图标）
│
├── frame/               # 历史版本设计文档（V1.0.1 ~ V1.0.6 各Part方案）
├── Instructions/        # 项目文档（版本更新记录/技术参数资料/游戏设计说明书/游戏指南与新手教程）
├── 项目宪章.md          # 本文件
│
├── build/               # PyInstaller 构建中间产物
├── dist/                # PyInstaller 打包输出（DungeonWarriors.exe）
├── DungeonWarriors.spec # PyInstaller 打包配置
├── font.otf / font.ttf  # 中文字体文件
└── .gitignore           # Git忽略规则
```

---
## 六、编码强制约束：空指针安全（最高优先级）
1. 本项目为肉鸽战斗游戏，运行时崩溃会直接破坏游戏体验，**禁止生成会引发空指针的代码**。
- 禁止直接对可空对象直接调用方法、访问属性；
- 所有引用类型变量，必须明确区分可空/非空，不允许模糊Object类型。

2. 存量旧代码处理规则
- 修改原有模块时：Agent必须扫描当前修改文件，识别潜在空指针风险，同步修复已存在空指针隐患，不能只改新功能忽略旧bug。
- 新增功能：不得复制旧代码的空指针错误模式。

3. 序列化相关强制规则（对应你之前遇到的：丢序列化、IDE补全失效）
- 所有需要序列化/存档的实体类：必须有无参构造；重要字段不能为隐式null；
- 反序列化返回对象，使用前必须做判空校验；禁止使用裸泛型，必须写明完整泛型参数，避免类型丢失导致IDE补全失效。

4. IDE可维护性硬性要求
- 保证类型明确，杜绝类型擦除导致IDE代码补全失效；
- 不写魔法对象、不使用动态弱类型绕过类型系统；
- 返回值尽量避免返回null，优先返回空集合、空对象替代null。

5. 提交输出检查
- Agent完成编码之后，必须自我检查：
- ①找出本批次代码中所有可能null的位置
- ②列出风险点清单，输出给用户确认
- ③对风险点增加判空保护，再输出最终代码。

### 禁止行为清单
- ❌ 禁止直接调用 `obj.method()`，obj可能为null
- ❌ 禁止反序列化后不做判空直接使用对象
- ❌ 禁止裸泛型、丢失类型，造成IDE补全失效
- ❌ 禁止忽略旧文件中的空指针隐患，只写新代码


## 七、贡献指南

### 开发流程

1. **Fork 仓库**
2. **创建功能分支**：`git checkout -b feature/xxx`
3. **开发并测试**
4. **提交代码**：遵循提交规范
5. **推送分支**：`git push origin feature/xxx`
6. **创建 Pull Request**

### 代码审查

- 确保代码符合命名规范
- 确保功能完整且无明显Bug
- 确保注释清晰
- 确保提交信息规范

### 问题反馈

- 使用 GitHub Issues 报告Bug
- 提供详细的复现步骤
- 附上截图或日志（如有）

---

## 八、许可证

本项目为个人练习项目，仅供学习交流使用。

---

**最后更新**: 2026年9月1日  
**当前版本**: V1.0.6.2（V1.0.6.2 头目BOSS血量+100% / V1.0.6.1 生命上限惩罚规则 / V1.0.6 顶部BOSS血条系统 / V1.0.5.12~21 连击&战斗特效、试炼刷怪笼强化与多笼机制、音乐与胜利界面调整）  
**维护者**: Dusk_Wind  
**所属**: 重庆大学大数据与软件学院
